# =============================================================================
#  Dockerfile API — Scoring Credit v2.0 (Render compatible)
#  Auteur    : Karl Bifu Batunguni
#  Copyright : (c) 2026 Bifu Albert Bank
# =============================================================================
FROM python:3.11-slim

WORKDIR /app

# Installer les outils de compilation nécessaires pour SHAP
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    g++ \
    build-essential \
    libgomp1 \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY api/ ./api/
COPY models/ ./models/
COPY data/ ./data/
RUN mkdir -p /app/data

EXPOSE 8000
HEALTHCHECK CMD curl -f http://localhost:8000/health || exit 1

CMD ["uvicorn", "api.main_v2:app", "--host", "0.0.0.0", "--port", "8000"]
